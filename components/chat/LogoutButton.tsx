import React from 'react';
import { Button } from '../ui/button';

export function LogoutButton() {
  return (
    <Button className="w-[80%] hover:scale-105 hover:font-bold transition-all ">
      Log out
    </Button>
  );
}
